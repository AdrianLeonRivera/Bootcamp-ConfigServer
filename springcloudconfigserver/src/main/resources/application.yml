server:
  port: 8888

spring:
  application:
    name: myvalue
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AdrianLeonRivera/Bootcamp-ConfigServer.git
          default-label: main
          search-paths:
            - '*service'
